<!--index.wxml-->
<text>测试</text>

<view class="list">
  <block wx:for="{{list}}">
    <navigator url="{{item.url}}" class="list-item">{{item.text}}</navigator>
  </block>
</view>
<footer bindchooseTest="changeCurrent" chooseCurrent="{{current}}"></footer>